//import { TestBed, async } from '@angular/core/testing';  // 1
//import { AppComponent } from '../app/app.component';
//import { LoggerComponent } from '../app/logger/logger.component';

/*
describe('XXX', () => {
  it('should exist', function () {
    expect({ h: 7 }).toEqual({ h: 7 });
  })
});

describe('AppComponent', () => {
  beforeEach(async(() => { // 3
    TestBed.configureTestingModule({ // 4
      declarations: [
        AppComponent
      ],
    }).compileComponents(); // 5
  }));
  //
});
*/
//});


/*
describe('rrr', () => {
  var loggerComponent;
  let comp;

  beforeEach(async(() => {
    loggerComponent = TestBed.createComponent(LoggerComponent);
    comp = loggerComponent.componentInstance;
  }));

  it('333', () => {
    expect(comp.dataSource).toBeTruthy();
  });

  it('444', () => {
    expect(comp.dataSource).toEqual(loggerComponent);
  });
  console.log('777');


});

*/




